import React from 'react';
import TodoList from './TodoList';

const Todos = () => {
    return (
        <div>
            <h1>할일 만들기</h1>
            <TodoList/>
        </div>
    );
};

export default Todos;